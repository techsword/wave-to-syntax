# Runtime dependencies for this repository live in pyproject.toml
# ([project].dependencies, PEP 621). That file is the single source of truth.
#
# Environment for the code behind "Wave to Syntax: Probing spoken language
# models for syntax" (Interspeech 2023, arXiv 2305.18957).
#
# Recommended (uv), select one accelerator extra:
#   uv sync --python 3.10 --extra cpu
#   uv sync --python 3.10 --extra cu121
#
# pip: install the package into an active environment, then install
# torch/torchaudio from the matching PyTorch index, for example:
#   pip install -e .
#   pip install torch==2.5.1 torchaudio==2.5.1 \
#       --index-url https://download.pytorch.org/whl/cpu
